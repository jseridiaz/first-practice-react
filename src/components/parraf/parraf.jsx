const Parraf = (props) => {
  //* const [number, setNumber] = React.useState('0')

  //* React.useEffect(() => {
  //*   setNumber('0')
  //* }, [])

  return <h2>Number clicks {props.number}</h2>
}

export default Parraf
